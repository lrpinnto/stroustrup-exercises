// CHAPTER 24 EX 1

// The function arguments f for a.apply(f,a) are different. Write a triple() function for each and use each to triple the elements of an array 
// {1 2 3 4 5}. Define a single triple() function that can be used for both a.apply(triple) and apply(triple,a). Explain why it could be a bad idea
// to write every function to be used by apply() that way.