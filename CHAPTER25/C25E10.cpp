// CHAPTER 25 EX 10

// Look at the bitfield example from 25.5.5. Write an example that initializes a PPN, then reads and prints each field value, then changes each
// field value (by assigning to the field) and prints the result. Repeat this exercise but store the PPN information in a 32 bit unsigned int
// and use bit manipulation operators to access the bits in the word